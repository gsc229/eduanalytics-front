import { createTheme } from "@material-ui/core"

const theme = createTheme({
  palette: {
    primary: {
      main: "#004545"
    },
    secondary: {
      main: "#CBDB3F"
    }
  }
})

export default theme;